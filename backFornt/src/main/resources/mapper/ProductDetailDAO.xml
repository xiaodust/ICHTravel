<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icht.backfront.dao.ProductDetailDAO">
    <resultMap id="BaseResultMap" type="com.icht.backfront.dataobject.ProductDetailDO">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="product_id" jdbcType="VARCHAR" property="productId"/>
        <result column="price" jdbcType="DECIMAL" property="price"/>
        <result column="size" jdbcType="DOUBLE" property="size"/>
        <result column="stock" jdbcType="INTEGER" property="stock"/>
        <result column="gmt_created" jdbcType="TIMESTAMP" property="gmtCreated"/>
        <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified"/>
    </resultMap>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_detail (id, product_id, price, size, stock, gmt_created, gmt_modified) VALUES
        (#{id}, #{productId}, #{price}, #{size}, #{stock}, #{gmtCreated,jdbcType=TIMESTAMP}, #{gmtModified,jdbcType=TIMESTAMP})
    </insert>
    
    <update id="updateById">
        UPDATE product_detail
        <set>
            <if test="productId != null">
                product_id=#{productId},
            </if>
            <if test="price != null">
                price=#{price},
            </if>
            <if test="size != null">
                size=#{size},
            </if>
            <if test="stock != null">
                stock=#{stock},
            </if>
            gmt_modified=now()
        </set>
        WHERE id=#{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM product_detail WHERE id=#{id}
    </delete>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, product_id, price, size, stock, gmt_created, gmt_modified
        FROM product_detail
        WHERE id=#{id}
    </select>
    
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT id, product_id, price, size, stock, gmt_created, gmt_modified
        FROM product_detail
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, product_id, price, size, stock, gmt_created, gmt_modified
        FROM product_detail
    </select>
    
    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT id, product_id, price, size, stock, gmt_created, gmt_modified
        FROM product_detail
        WHERE product_id=#{productId}
    </select>
</mapper>